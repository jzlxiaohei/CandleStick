var W = function (selection, options) {
    this._selection = selection;
    this._options = options;
    this._init();
};

W.prototype = {
    _component: {},
    _chart: {},

    _init: function () {

    },

    addComponent: function (key, options) {

    }
}
